language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-burk' ]; then cd .. && eval "mv $currentfolder generator-burk" && cd generator-burk; fi
